<h1 class="center-align">Realms</h1>

<div class="collection">
    <% realms.forEach(realm => { %>
        <a href="/realms/<%= realm.slug%>" class="collection-item"><%= realm.name %></a>
    <% }) %>
</div>